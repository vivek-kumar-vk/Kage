# Shared local infrastructure the whole project leans on.
#
# Installed by Start_Inky\Start_Everything.bat alongside each screen's
# own requirements. The Postgres binaries are NOT pip-installable - they
# come from the EDB installer on Windows / `pkg install postgresql` on
# Termux; Tools/manage_postgres.py only drives them.
#
# PYTHON: build the .venv with 3.11-3.13. Under 3.14 `import prisma`
# (the gateway's Postgres client) hangs on import. Start_Everything.bat
# auto-picks a compatible interpreter when one is installed.

# The local model gateway: routing, fallback, spend logging, admin UI.
# [proxy] pulls in fastapi/uvicorn for the server; prisma is the ORM the
# proxy uses to talk to Postgres and is listed explicitly because recent
# litellm releases stopped pinning it in the [proxy] extra.
litellm[proxy]>=1.98
prisma>=0.15
