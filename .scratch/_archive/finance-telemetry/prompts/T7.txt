Write ONE React component file. Stack: Next.js 16 app router, React 19, TypeScript,
Tailwind v4 utility classes, framer-motion 13. Do NOT add dependencies. Do NOT use Three.js.

FILE: app/components/TotalBalanceReadout.tsx

CONTEXT — these already exist, import them exactly:
  import { BLUEPRINT_SEED } from "@/app/lib/blueprintSeed";
  import { useCountUp } from "@/app/lib/useCountUp";
  import { formatINR } from "@/app/lib/formatINR";

TAILWIND CLASSES AVAILABLE (already wired to the theme):
  bg-void bg-panel bg-raised  border-line  text-bone text-dim text-amber
  class "num" = monospace tabular figures (put it on every number)
  class "carbon-face" = carbon-fibre texture background
  class "checkered-sweep" = amber checker hover sweep (host must be relative; it is, via the section)

WHAT IT RENDERS:
  export function TotalBalanceReadout()
  A <section aria-label="Total balance"
     className="rubric-panel carbon-face checkered-sweep relative overflow-hidden rounded-lg border border-line p-5">
  Inside:
    - <p className="num text-[10px] tracking-[0.2em] text-dim">TOTAL BALANCE</p>
    - the amount: const shown = useCountUp(BLUEPRINT_SEED.totalBalance);
      <p className="num mt-1 text-4xl font-bold text-amber">{formatINR(Math.round(shown))}</p>
    - a thin bottom accent bar that grows on mount:
      <motion.div className="mt-4 h-[3px] rounded bg-amber/70"
        initial={{ scaleX: 0 }} animate={{ scaleX: 1 }}
        style={{ transformOrigin: "left" }}
        transition={{ duration: 1.1, ease: [0.16, 1, 0.3, 1] }} />
  Import { motion } from "framer-motion".
  First line: "use client";
  Start with a one-sentence /** ... */ doc comment.
  Named export only. No default export.

Return ONLY the file contents inside one ```tsx code block. No explanation.
