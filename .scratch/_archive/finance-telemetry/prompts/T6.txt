Write ONE React component file. Next.js 16, React 19, TypeScript, Tailwind v4, framer-motion 13.
No new dependencies. No Three.js. No red colours (Rule 8).

FILE: app/components/SpeedoNav.tsx

KEEP THESE FIRST LINES EXACTLY (verbatim, including quotes):

"use client";

import { motion, useReducedMotion } from "framer-motion";

type Item = { id: string; label: string };

/** The left-rail nav, styled as a speedo/steering-wheel cluster: an
    arc "wheel edge", one node per tab, and a needle that springs to the
    active node. Drives the parent's tab state. */
export function SpeedoNav({
  items,
  tab,
  onSelect,
}: {
  items: Item[];
  tab: string;
  onSelect: (id: string) => void;
}) {

THEN implement the body EXACTLY like this:
  const reduce = useReducedMotion();
  const NODE_Y = [50, 150, 250, 350, 440];
  const active = Math.max(0, items.findIndex((it) => it.id === tab));
  const activeY = NODE_Y[Math.min(active, NODE_Y.length - 1)];

  return (
    <nav aria-label="Finance sections" className="speedo-nav w-16 shrink-0">
      <svg viewBox="0 0 64 480" className="h-full w-full" role="presentation">
        {/* wheel edge */}
        <path d="M 10 24 Q 2 240 10 456" fill="none" stroke="var(--sumi-line)" strokeWidth={2} />
        {/* needle */}
        <motion.line
          x1={10}
          y1={240}
          x2={26}
          stroke="var(--amber)"
          strokeWidth={2}
          strokeLinecap="round"
          initial={{ y2: reduce ? activeY : NODE_Y[0] }}
          animate={{ y2: activeY }}
          transition={reduce ? { duration: 0 } : { type: "spring", stiffness: 120, damping: 18 }}
        />
        {items.map((it, i) => {
          const y = NODE_Y[Math.min(i, NODE_Y.length - 1)];
          const on = i === active;
          return (
            <g key={it.id} className="checkered-sweep" style={{ cursor: "pointer" }} onClick={() => onSelect(it.id)}>
              <rect x={0} y={y - 30} width={64} height={60} fill="transparent" />
              <circle
                cx={26}
                cy={y}
                r={5}
                fill={on ? "var(--amber)" : "var(--ink-void)"}
                stroke={on ? "var(--amber)" : "var(--sumi-line)"}
                strokeWidth={1.5}
              />
              <line x1={34} y1={y} x2={on ? 46 : 42} y2={y} stroke={on ? "var(--amber)" : "var(--sumi-line)"} strokeWidth={1.5} />
              <text
                transform={`rotate(-90 52 ${y})`}
                x={52}
                y={y}
                textAnchor="middle"
                dominantBaseline="middle"
                fontSize={7}
                letterSpacing={1}
                fill={on ? "var(--bone)" : "var(--bone-dim)"}
                style={{ fontFamily: "var(--font-data)" }}
              >
                {it.label}
              </text>
            </g>
          );
        })}
      </svg>
    </nav>
  );
}

Return ONLY the file contents inside one ```tsx code block. No explanation.
