Write ONE TypeScript file. No React, no imports, no dependencies.

FILE: app/lib/formatINR.ts

REQUIREMENTS:
- Export a function: export function formatINR(n: number): string
- Round n to the nearest integer.
- Format with Indian digit grouping (e.g. 150000 -> "1,50,000", 87500 -> "87,500").
- Prefix with the rupee sign "₹". Example: formatINR(150000) === "₹1,50,000".
- Handle negatives: formatINR(-1700) === "-₹1,700" (minus sign before the ₹).
- Implementation: use n.toLocaleString("en-IN", { maximumFractionDigits: 0 }).
- Also export: export function formatINRCompact(n: number): string
  which returns "₹1.5L" for 150000, "₹2.4L" for 240000, "₹24.0L" for 2400000,
  and falls back to formatINR(n) for values under 1000. Use 1 decimal place for the L form.
- Start the file with a short /** ... */ doc comment (one sentence).

Return ONLY the file contents inside one ```ts code block. No explanation.
