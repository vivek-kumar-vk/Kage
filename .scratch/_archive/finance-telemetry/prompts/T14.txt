Write ONE React component file. Next.js 16 app router, React 19, TypeScript, Tailwind v4,
framer-motion 13. No new dependencies. No Three.js.

FILE: app/components/PortfolioReadout.tsx

IMPORT EXACTLY:
  import { BLUEPRINT_SEED } from "@/app/lib/blueprintSeed";
  import { useCountUp } from "@/app/lib/useCountUp";
  import { formatINR } from "@/app/lib/formatINR";
  import { motion } from "framer-motion";

TAILWIND CLASSES AVAILABLE: bg-void bg-panel border-line text-bone text-dim text-amber
  class "num" = monospace tabular (put on every number)
  class "carbon-face" = textured background   class "checkered-sweep" = hover sweep

STRUCTURE:
  export function PortfolioReadout()
  <section aria-label="Portfolio value"
    className="carbon-face checkered-sweep relative overflow-hidden rounded-lg border border-line p-5">
    <p className="num text-[10px] tracking-[0.2em] text-dim">PORTFOLIO VALUE</p>
    const shown = useCountUp(BLUEPRINT_SEED.portfolioValue);
    <p className="num mt-1 text-3xl font-bold text-amber">{formatINR(Math.round(shown))}</p>
    <p className="num mt-1 text-[10px] text-dim">current market value</p>
    <motion.div className="mt-4 h-[3px] rounded bg-amber/70"
      initial={{ scaleX: 0 }} animate={{ scaleX: 1 }}
      style={{ transformOrigin: "left" }}
      transition={{ duration: 1.1, ease: [0.16, 1, 0.3, 1] }} />
  </section>

First line: "use client";
Start with a one-sentence /** ... */ doc comment.
Named export only, no default.

Return ONLY the file contents inside one ```tsx code block. No explanation.
