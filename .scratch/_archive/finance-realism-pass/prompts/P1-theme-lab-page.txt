You are Model A, writing ONE file for a Next.js 16 (App Router, `output: "export"`)
+ React 19 + Tailwind v4 + framer-motion ^13 project. Patched Next — follow the
shapes in THIS prompt, not your training data.

RULES (contract):
- First line of the file: "use client";
- Colours: use ONLY the CSS custom properties this prompt defines (var(--tl-*)).
  No raw hex anywhere in JSX/CSS you write except inside the THEMES data object
  below. Never use red as decoration outside an "accent" slot.
- No chart/graph/3D library. Any visualization is hand-rolled SVG.
- Any animation must be disabled under prefers-reduced-motion.
- Output the file body ONLY. No prose, no explanation. One optional ```tsx fence.
- Keep it under ~120 lines.

FILE: Screens/Finance/Page/next_app/app/theme-lab/page.tsx

PURPOSE: a throwaway theme-exploration route. Renders one sample "stage" under a
selectable colour theme, with a fixed floating bottom bar to switch themes. This
is a PROTOTYPE — not production.

REQUIREMENTS:
1. Default export `ThemeLab` (a client component).
2. A `THEMES` const array with 4 entries. Each: `{ id: string; name: string;
   vars: Record<string,string> }`. The `vars` keys, on every theme, are exactly:
   `--tl-bg`, `--tl-bg-2`, `--tl-surface`, `--tl-line`, `--tl-text`,
   `--tl-text-dim`, `--tl-accent`, `--tl-accent-2`, `--tl-accent-soft`,
   `--tl-glow`, `--tl-petal`.
   Fill the 4 themes as 4 DISTINCT takes on "Red Bull F1 (deep navy + red +
   yellow) + drifting sakura + evening tone", e.g.:
   - id "navy-sharp": navy-dominant, red as a sharp accent, yellow rare, crisp.
   - id "evening-warm": deep plum/maroon horizon feel, red-orange accent, softer.
   - id "pitwall": near-black base, high-contrast electric red + bright yellow.
   - id "dusk-calm": desaturated navy-grey, one warm accent, lots of calm
     (closest to a quiet goals screen).
   Use hex values inside `vars` (this is the one allowed place).
3. State: `const [idx, setIdx] = useState(0)`. On mount, if
   `location.hash` matches a theme id, select it. When a theme is picked, set
   `location.hash` to its id.
4. Layout: a full-viewport `<div>` whose `style` spreads the active theme's
   `vars` plus `background: "var(--tl-bg)"`, `color: "var(--tl-text)"`,
   `minHeight: "100dvh"`. Inside:
   - `<ThemeLabStage />` imported from `"./stage"` (assume it exists and takes no
     props; it reads the var(--tl-*) values itself).
   - a `position: fixed` bottom-centre bar: one `<button>` per theme showing
     `theme.name`; the active one visually marked using `var(--tl-accent)` and
     `var(--tl-glow)`. The bar uses `var(--tl-surface)` / `var(--tl-line)`.
5. No data fetching. No external imports beyond `react`.
