You are Model A, writing ONE file for Next.js 16 (App Router, output:"export") +
React 19 + Tailwind v4 + framer-motion ^13. Patched Next — follow THIS prompt.

CONTRACT:
- The literal first line is these 8 characters then a semicolon: "use client";
  WITH the double quotes. `use client;` is a bug.
- Use ONLY these CSS custom properties for colour, via var(--name): --tl-bg,
  --tl-bg-2, --tl-surface, --tl-line, --tl-text, --tl-text-dim, --tl-accent,
  --tl-accent-2, --tl-accent-soft, --tl-glow. No raw hex anywhere.
- No chart/graph library. The sparkline is hand-rolled inline <svg> with a
  <path>.
- Any animation disabled under prefers-reduced-motion.
- Output the file body ONLY, no prose. Under ~120 lines.

FILE: Screens/Finance/Page/next_app/app/theme-lab/stage.tsx

PURPOSE: the sample content shown inside the theme-lab prototype so a human can
judge each colour theme on realistic UI. PROTOTYPE code.

REQUIREMENTS:
1. Default export `ThemeLabStage`, a client component, no props. It reads colours
   only through var(--tl-*), which an ancestor sets.
2. Root: a responsive flex/grid, max-width 920px, centered, padding 32px, with
   gap 20px. A relative z-index above the petals (e.g. position:"relative",
   zIndex:1).
3. Left: a vertical nav slice — 4 items ["Overview","Investments","Debt",
   "Portfolio"], the first one active. Active item: text var(--tl-text), a
   2px left border var(--tl-accent), subtle background var(--tl-surface).
   Inactive: var(--tl-text-dim).
4. Main: two cards, each background var(--tl-surface), border 1px var(--tl-line),
   borderRadius 14, padding 20.
   - Card A "TOTAL BALANCE": a small dim label, then a big number
     "₹1,50,000" (fontSize ~34, fontWeight 700, color var(--tl-text)); to its
     right a tiny "+4.2%" in var(--tl-accent-2). Below: a hand-rolled SVG area
     sparkline, width 100% (viewBox "0 0 240 60"), from this series
     [180,176,190,205,198,214,222,219,231,228,236,240]: a <path> stroke
     var(--tl-accent) strokeWidth 2 fill none, plus a second <path> for the
     filled area under it using fill var(--tl-accent) at opacity 0.15. Map the
     12 points evenly across x 0..240, y inverted into 6..54 by min/max of the
     series. // SAMPLE data — hard-code it, no imports.
   - Card B "SAVINGS RATE": label, then a horizontal bar meter — a track
     (height 10, borderRadius 999, background var(--tl-bg-2)) with a fill at 68%
     width, background var(--tl-accent), boxShadow "0 0 12px var(--tl-glow)".
     Under it a dim "68% of income kept".
5. No external imports beyond react. No framer-motion needed here.
