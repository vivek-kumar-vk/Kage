Write ONE file: a React hook. Stack: Next.js 16, React 19, framer-motion 13.1.1.

FILE: app/lib/useCountUp.ts

REQUIREMENTS:
- First line exactly: "use client";
- Import { animate, useReducedMotion } from "framer-motion".
- Import { useEffect, useState } from "react".
- Export: export function useCountUp(target: number, durationMs = 1100): number
- Behaviour:
  - const reduce = useReducedMotion();
  - state `value`, initialised to `reduce ? target : 0`.
  - In a useEffect keyed on [target, durationMs, reduce]:
    - if `reduce` is true: setValue(target); return;
    - otherwise call:
        const controls = animate(0, target, {
          duration: durationMs / 1000,
          ease: [0.16, 1, 0.3, 1],
          onUpdate: (v) => setValue(v),
        });
      and return () => controls.stop();
  - return `value`.
- Start with a one-sentence /** ... */ doc comment.
- No other exports, no JSX.

Return ONLY the file contents inside one ```ts code block. No explanation.
