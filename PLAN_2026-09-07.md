# PLAN — 2026-09-07 · implementing Fable's BUILD_ORDER.md

Source of truth: `C:\Users\vkjha\Project-Audit-OUT\BUILD_ORDER.md` (25 above-the-cut-line
tickets K-00…K-24, 19 below-the-line B-01…B-19). This file is the execution state; the
ticket-by-ticket detail (exact changes, deviations, red-test results) lives in
`OVERNIGHT_LOG.md`. Everything below is committed locally — **nothing pushed** (owner
reviews locally; push is owner's via B-01).

## Progress

- **Above the cut line: 18 / 25 done = 72%.**
- **Below the cut line: 1 / 19 (B-02).**
- Whole plan (44 tickets): 19 / 44 ≈ **43%**.
- The build is **decision-blocked, not work-blocked**: every remaining above-line ticket
  waits on one owner answer (K-06), and below-line GLM work continues under it.

## Shipped (above the cut line) — one commit each, regression green after every ticket

| Ticket | What shipped | Commit |
|---|---|---|
| K-00 | Root `requirements.txt` pinned from the working venv (62 pkgs); fresh-venv validated | `063c9b4` + `45d19df` |
| K-01 | `Start_Inky/run_checks.py` covers all 7 screens + 2 hygiene gates, three-valued statuses | `5573a7d` |
| K-02 | Verified already satisfied by Z3 (evidence logged, no code change) | `0adc04d` |
| K-03 | Event spine write path `Shared_By_All_Screens/spine.py` — schema-before-IO, O_EXCL lock (Windows PermissionError retry), fsync, loud SpineWriteError | `8756c77` |
| K-04 | Spine projection into `spine.sqlite` — migrations, idempotent byte-offset replay, freshness views (`julianday('now')` fix proven against pinned fixture) | `1bae632` |
| K-05 | Seven spine read endpoints on Storage, project-first, 503 on failure | `28f38c0` |
| K-08 | LLM seam rewrite — `_routing.json` chains, one attempt + one schema repair, single `llm_call` event incl. failure paths | `2e7f2d0` |
| K-09 | Budget gate + real cost from usage — no-price refusal (D-05), caps day/agent/call/T2-share, T0 never blocked | `debdab6` |
| K-10 | Sanitisation inside the seam — reversible `[[KIND_n]]` tokens, one mapping per ask, unknown tokens listed never guessed | `e8beb8d` |
| K-12 | Finance freshness events + dated honest cache — per-source tallies, no `price_last_refresh` writes | `c457026` |
| K-13 | Calendar/WakaTime/Gmail syncs emit spine fetch events (attempted/succeeded/failed) | `9f2f1ea` |
| K-14 | Calendar tz root-cause — IST windows/days/clocks, honest spine-read freshness | `ddf81fe` |
| K-15 | Calendar card presentation — IST clocks, ages not bare clocks, stale amber footer (npm build green) | `bb45945` |
| K-16 | One XIRR — float bisection on ISO-date flows; ticket fixture values corrected with proofs | `c3b10da` |
| K-17 | Finance honesty — absent is None, unpriced excluded from totals and listed, observations not advice, reads never fetch | `b93f7ea` |
| K-18 | Board one-in-one-out at 12 + closes + `ticket_opened`/`ticket_closed` spine events | `4c917fa` |
| K-19 | Watchdog on the Main Menu tick — verdicts only, marker-file 30-min budget, cannot_tell never up | `c08a90c` |
| K-23 | Ingestion adapter — Pomodoro/Books/YouTube inbox → spine, sha256 idempotency, honest rejections, books library + read cursor | `1abe2de` |

Auth record: owner's mid-run override "U do sonnet part and don't stop" committed as
`ef464af`; the first-pass SONNET-skip accounting is `0adc04d`/`4b88237`.

## Parked — one owner decision unblocks four tickets

**K-06 is the blocker.** Implementing it conflicts with the shipped ask-path
`data_sources` feature (`test_read_office_roundtrip`, commit `f901a44`). All K-06 changes
were reverted (`137659d`). Until the owner picks:

- **Keep `data_sources`** → K-06 stays out → K-07, K-11, K-24 stay parked → K-20, K-21,
  K-22 stay blocked (they need K-07's fleet wiring). B-08 (first A2 consumer) waits too.
- **Let K-06 replace it** → the chain unblocks and 7 tickets move.

## Below the cut line — state and queue

Shipped: **B-02** — demo generator deleted (`4cabd81`); `events.py` 266→131 lines,
lifespan wiring removed, two red tests added; the stage feed has real producers only.
The `DEMO_EVENTS` lines in `settings_for_agents.py` stay (protected port settings file;
nothing reads them). GLM-executable queue, dependency-ordered:

1. **B-03** `generate_structure_docs.py` + Storage `structure.py` router (2 evenings)
2. **B-04** Storage trigger cards + drawers (needs B-03; visual check)
3. **B-07** Books shelf + upload + read-progress on Storage (contract K-23 shipped)
4. **B-06** Email card compact presentation (cosmetic; visual check)
5. **B-13** Pomodoro tray app (producer only; K-23 adapter exists)
6. **B-17** Wire A4–A7 to their endpoints (endpoints to verify first)

Not startable now: B-01 (owner: `gh` install), B-05 (Sonnet + D-12), B-08 (K-07 parked),
B-09/B-14 (Sonnet), B-10 (D-15), B-11 (D-10), B-12 (D-06), B-19 (D-07), B-15 (owner must
want Playwright), B-16 (needs a week of real data), B-18 (after B-09).

## Owner decisions outstanding

1. **K-06 vs `data_sources`** (unblocks 7 tickets — the big one)
2. **D-05** model prices — the budget gate currently refuses unpriced models by design
3. **D-15** YouTube source (Data API vs Takeout) for B-10
4. **D-16/D-17** (inside parked K-24)
5. **D-10/D-12/D-06/D-07** gate B-11/B-05/B-12/B-19 respectively
6. **K-15 visual pass** — one minute with the calendar card open
7. **D-14 half-resolved** — rotate module exists, `test_the_rules_are_followed.py` does not; owner says whether to write it
8. **B-15** — does the owner want the Playwright harness at all?

## Standing rules honored all run

Never pushed, no PR. Port settings files (`settings_for_*.py`), launcher, and screen
discovery untouched. No value ever invented — absent renders absent and dated. Only
permitted failure: `test_current_data_block_truncates_oversized` (Agents). Regression
`.venv\Scripts\python Start_Inky\run_checks.py` after every ticket; the full log with
deviations is `OVERNIGHT_LOG.md`.
