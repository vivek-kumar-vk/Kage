# Overnight build plan — 2026-09-06 (owner asleep, Claude autonomous)

Ordered hardest/longest first. One task at a time, commit after each step, test before
committing. This file is the working checklist for tonight only (the four root docs rule
stands — it gets cleaned up in the morning; the decision record goes to AGENTS.md).

Ground truth gathered before writing this plan:
- Agent Deck frontend: `Screens/Agents/Page/next_app` (Next 16 static export → `out/`,
  served by FastAPI at :8004; must `npm run build` after edits).
  Roster: `components/deck/DeckRail.tsx` (grouped by department, TIER_RANK sort).
  Default view: `app/workspace/page.tsx:24` → `{kind:"room", id:"board"}`.
  Presence dot pulse: `app/globals.css:337-360` (`.presence-working` → `deck-pulse`).
  DM fetch/send: `GET/POST /api/agents/agents/{name}/messages` (`AgentChat.tsx`).
  `office.json` `tier`/`parent` already exposed in `/api/agents/workspace`.
- Main Menu: `Main_Menu/Page/next_app` (Next 16 static export at :8000).
  Ring exists but decorative: `app/components/AgentRing.tsx` (30 hard-coded NODES,
  spinning, no hover/click) inside `CenterCore.tsx`. Theme: `#0f0f0f` bg, amber
  `#ff7a00`, Chakra Petch / IBM Plex. Backend must not name other screens — discovery
  via `find_every_screen.py`.
- Model screen: `Screens/Model/Page/page_for_model.html` (87-line hand-rolled page,
  inline CSS `#0b0d10`, monospace) + `Backend/server_for_model.py` (only
  `/api/model/overview`). No chat, no gateway proxy, key stays server-side in
  `settings_for_model.py`.

---

## 1. Unread-message spine (Agents backend) — hardest prerequisite, everything hangs off it

- [ ] 1.1 Read `Backend/schema.sql` + `services/agents.py` message insert paths to learn
      message identity (role/sender column) — unread = messages not authored by the owner
      newer than the last-read marker.
- [ ] 1.2 Migration (idempotent, like D35 pattern): table `message_reads`
      (`room_id TEXT PRIMARY KEY, last_read_id INTEGER NOT NULL DEFAULT 0,
      updated_at TEXT`). Never touches `messages` (append-only, D27.2).
- [ ] 1.3 `GET /api/agents/unread` → `{agents: {name: n}, rooms: {room_id: n}, total: n}`
      computed per agent room: count of messages with `id > last_read_id` and not
      owner-authored. Missing row = everything unread.
- [ ] 1.4 `POST /api/agents/rooms/{room_id}/read` body `{last_read_id}` (clamps to max
      message id) — upsert into `message_reads`.
- [ ] 1.5 Tests in `Backend/tests/` (run_checks picks the suite up): unread counts on
      seeded rooms, read-marker clamp, missing-row behaviour. Run `run_checks.py`.
- [ ] 1.6 Commit.

## 2. Agent Deck roster: parent-collapsible + Slack-style unread numbers

- [ ] 2.1 `DeckRail.tsx`: within each department, group subs under their `parent` main;
      mains/head render as collapsible section headers (chevron, persisted collapse state
      in localStorage), subs indented under them. All 37 agents render (head, then mains
      with their crews; Anime_Main_Agent keeps its own department group).
- [ ] 2.2 Unread badge: numeric chip per agent row (like Slack), from
      `GET /api/agents/unread` polled alongside workspace load + on SSE message events.
      Remove the infinite `deck-pulse` blink on roster presence dots (keep the static
      colour states; typing indicator keeps its blink — that's live feedback, not unread).
- [ ] 2.3 Read-marker write: when an agent chat is open and at bottom / on new messages
      rendered, POST the read marker so the badge clears.
- [ ] 2.4 `npm run build`, verify at :8004 in browser, commit.

## 3. Agent Deck default = orchestrator chat

- [ ] 3.1 `app/workspace/page.tsx:24` → default selection `{kind:"agent", id:"Deck_Main_Agent"}`
      (the orchestrator). Guard the loading state so the first paint doesn't flash
      "Agent not found" while `agents` is still empty.
- [ ] 3.2 Opening the deck or a chat marks it read (2.3 covers the chat path).
- [ ] 3.3 Build, verify, commit.

## 4. Main Menu: real agent-node ring

- [ ] 4.1 Backend proxy `GET /api/main_menu/agents` in `server_for_main_menu.py`:
      resolve the Agents screen address via the same discovery the navigation endpoint
      uses (never hardcode 8004 — file ADR), fetch `/api/agents/workspace` +
      `/api/agents/unread` server-side, return a compact list:
      `{name, department, tier, parent, model, unread, room_url}`. Agents screen down →
      honest `state: "agents offline"`, empty nodes, no fabricated rows.
- [ ] 4.2 `AgentRing.tsx`: replace decorative NODES with real agent nodes — one small
      circle per agent arranged in the ring; mains on the inner ring, subs on the outer
      ring (or one ring ordered head→mains→subs). Main-model node bigger with its model
      name rendered just below it inside the ring. Hover → name tooltip (DOM overlay,
      not canvas). Click → navigate to `:8004` deep-linked to that agent's chat
      (`/workspace?agent=<name>` — Agent Deck reads the query param into selection).
- [ ] 4.3 Unread glow: node carries an amber glow when `unread > 0` (CSS box-shadow,
      animated softly; respects `prefers-reduced-motion`).
- [ ] 4.4 `app/workspace/page.tsx` + `DeckRail`: read `?agent=` param on load and select
      that agent's DM as the initial view.
- [ ] 4.5 `npm run build` both apps; verify ring at :8000 and deep-link at :8004; commit.

## 5. Model page: orchestrator chat with / commands (backend)

- [ ] 5.1 `server_for_model.py`: `POST /api/model/chat` — body `{message}` (plain chat
      with the orchestrator agent via the Agents screen ask path) and `{message,
      target}` (`/`-command path). Two server-side forwards:
      plain → `POST <agents>/api/agents/agents/Deck_Main_Agent/ask`;
      `/name message` → `POST <agents>/api/agents/agents/<name>/ask`.
      Agents address resolved from settings/discovery, key never leaves the process
      (D25.3 posture), returns `{state, reply, agent}` honestly on failure (D27.1 shape).
- [ ] 5.2 `/all <message>` → ask every main-tier agent in sequence, return array of
      replies (bounded, per-agent state). Cap total response size.
- [ ] 5.3 Tests where testable without the gateway (routing/validation), commit.

## 6. Model page: chat UI + glassy look (frontend)

- [ ] 6.1 `page_for_model.html`: small chat panel docked bottom-right (~360×300px,
      scrollable transcript, one input). `/`-command autocomplete hint line listing
      agent names (fetched from the agents proxy via the Model backend — add
      `GET /api/model/agents` passthrough so the page needs no cross-origin call).
- [ ] 6.2 Glass look: translucent rgba panels + `backdrop-filter: blur`,
      subtle 1px light border, soft gradient sheen — same dark palette tokens
      (`#0b0d10` family, existing accent colours), monospace kept.
- [ ] 6.3 Verify live at :8001 (gateway down → chat still renders with honest error),
      commit.

## 7. Sweep

- [ ] 7.1 Full `run_checks.py` green; all builds committed.
- [ ] 7.2 AGENTS.md decision line(s) for what shipped; PLAN.md item updated;
      NOW.md untouched (its task isn't this).
- [ ] 7.3 `OWNER_NEED_2026-09-06.md` kept current with anything only the owner can do.

## Standing constraints tonight

- No fabricated data anywhere (Rule 8): agents offline = stated, unread 0 = real 0.
- Runs table stays append-only; read-markers are a new table, never message edits.
- Keys server-side only; loopback only.
- Commit after every numbered step that builds and passes checks.
