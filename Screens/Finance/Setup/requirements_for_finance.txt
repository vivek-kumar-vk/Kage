# What the Finance screen needs installed.
#
# Every screen keeps its own list, so one screen can be installed and run
# on its own without dragging in the others. Start_Everything.bat installs
# every one of these files it can find.
#
# Install just this one:
#   cd <repo root>
#   python -m pip install -r Screens\Finance\Setup\requirements_for_finance.txt

# The small web server behind this screen.
fastapi>=0.110
uvicorn[standard]>=0.27

# Market facts off Yahoo Finance (fetch_market_facts.py): index quotes
# and stock prices the free Indian MF APIs do not carry. Configured
# 2026-08-22. Nothing at server startup imports it - only the market
# facts endpoints do, and they answer honestly if it is missing.
yfinance>=0.2

# Every calculation the RUNNING screen does is plain Python arithmetic -
# no pandas, no numpy. That is Tier 0, and it is the goal: if a formula
# can do it, no library and no AI model gets involved.
#
# The two below are the one exception, and they are used by exactly one
# file: Calculations/import_from_broker_statements.py, a one-time,
# run-by-hand script that reads a broker's xlsx export. Nothing the
# server imports at startup touches either of them.
pandas>=2.2
openpyxl>=3.1

# Reads a CAS (Consolidated Account Statement) PDF - units and current
# value only, matched against portfolio_holdings.csv. Used by exactly
# one file, run by hand: Calculations/Investments_Tab/pull_from_cas_statement.py.
# Same reasoning as pandas/openpyxl above: nothing the server imports at
# startup touches it.
casparser>=1.3
