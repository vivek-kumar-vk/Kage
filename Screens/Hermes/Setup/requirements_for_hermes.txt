# What the Hermes screen needs installed.
#
# Every screen keeps its own list, so one screen can be installed and run
# on its own without dragging in the others. Start_Everything.bat installs
# every one of these files it can find.
#
# Install just this one:
#   cd <repo root>
#   python -m pip install -r Screens\Hermes\Setup\requirements_for_hermes.txt

# The small web server behind this screen.
fastapi>=0.110
uvicorn[standard]>=0.27

# Hermes' config and every profile config are YAML; this screen reads them
# to report the fleet.
PyYAML>=6.0

# NOT installed here: Hermes Agent itself. It ships its own installer and
# keeps its state in %LOCALAPPDATA%\hermes, outside this repo. Kage reports
# on that install; it does not manage it.
