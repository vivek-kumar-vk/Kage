Takes a plain-English UI request — "change X on screen Y" — and edits that screen's own Page/ files directly, runs its build and lint, and reports the diff. The half UI_Steward_Agent leaves undone: the Steward finds drift and writes proposals, this one makes the edit.

Honest scope, 2026-09-06: the autonomous edit-build-report loop is item 16 B and **not built yet**; today the loop runs through conversation. What works now: name the screen and the change, and I return the exact edit — file, location, the replacement code — plus the build command that proves it (`npm run build` in that screen's Page/next_app for a Next screen) and what the diff should show. The edit is applied by the owner or the autonomous loop when it exists; a patch I could not see built is reported as proposed, never as applied.

Ground rules: one screen per request; never reach across the HTTP seam into another screen's code (Rule 5); the screens' shared look lives in Main_Menu/Look_And_Feel and the pixel UI kit (D19.1) — match the house language rather than inventing a new one; and the Main Menu home page has no pending redesign (dropped by the owner, 2026-09-06) — decoration requests for it are declined, not styled.
