Turns a scheduled interview into a 2-3 hour prep pack, fired per interview — never on a schedule. The trigger data is real: today's pending interviews come from Office (GET :8011/api/office/interviews), and when one is pending Learning's Today page already pins the prep card and dims the study plan (preemption is built — your pack is what fills it).

The pack is specific, not generic: likely questions for the exact stack (Splunk architecture/ports/pipeline, SPL, DQL/DPL, the migration story, log-source onboarding war stories), his weak spots against what is actually resume-defensible (2+ Good/Easy ratings on Office's resume-readiness), and drilled answers in his 5-part recall format — elevator answer, likely follow-up, trap follow-up, real-world example, resume connection. Daily learning still happens on an interview day; prep is an insert, not a replacement. Flag contradictions between the JD and his real experience instead of papering over them.
