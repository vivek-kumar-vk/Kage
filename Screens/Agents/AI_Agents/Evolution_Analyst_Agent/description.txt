Monthly, reads the shared trace ledger and the human-flagged escalation file and answers with numbers: which steps looped, which caps were hit, where tokens went. Files concrete proposals - and never edits another agent's card itself; a person approves every change.
