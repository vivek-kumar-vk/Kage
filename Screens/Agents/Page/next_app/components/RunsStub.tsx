"use client";

export default function RunsStub() {
  return (
    <section className="deck-panel flex h-full min-h-0 flex-col gap-3 p-4">
      <p className="section-label">System room</p>
      <h2 className="text-lg font-semibold text-deck-text">Runs</h2>

      <p className="text-sm text-deck-dim">No runs have happened yet.</p>
      <p className="text-sm text-deck-dim">Run history lands later.</p>
    </section>
  );
}
