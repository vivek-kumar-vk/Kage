# What the Model screen needs installed.
#
# Every screen keeps its own list, so one screen can be installed and run
# on its own without dragging in the others. Start_Everything.bat installs
# every one of these files it can find.
#
# Install just this one:
#   cd <repo root>
#   python -m pip install -r Screens\Model\Setup\requirements_for_model.txt

# The small web server behind this screen.
fastapi>=0.110
uvicorn[standard]>=0.27

# Nothing else. The overview endpoint talks to the model gateway
# over plain HTTP with the standard library - no client library, no
# database of its own. The gateway itself (OmniRoute or similar) is
# installed and managed separately, not here.

# Running this screen's test suite.
pytest==8.4.2
pytest-asyncio==1.4.0
