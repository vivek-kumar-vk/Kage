# ─────────────────────────────────────────────────────────────
# Kage — .gitignore
# Public code only. Everything private lives in Google Drive.
# ─────────────────────────────────────────────────────────────

# --- Python ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / frontend builds ---
node_modules/
**/next_app/out/
**/next_app/.next/
**/next_app/next-env.d.ts
*.tsbuildinfo
**/svelte_app/build/
**/svelte_app/.svelte-kit/
dist/

# --- OS / editor ---
.obsidian/
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# --- Real personal data (NEVER commit) ---
/kage-data/

# --- Secrets & credentials (NEVER commit) ---
Secrets_Keys/
.env
.env.*
!.env.example
google_service_account.json
*_service_account.json
credentials.json
token.json

# --- Private input data (NEVER commit) ---
My_Investement_details/
My_Investment_details/
Dump/

# --- Runtime-generated private data (NEVER commit) ---
**/Saved_Records/
Main_Menu/Backend/Trace_Ledger/*.jsonl
Shared_By_All_Screens/Current_Numbers/NEEDS_HUMAN.csv
Shared_By_All_Screens/Current_Numbers/WAITING_FOR_YOU.csv
Main_Menu/Backend/dev_auto_refresh_toggle.json
Shared_By_All_Agents/Lease_Board/
Shared_By_All_Agents/*_state.json
Shared_By_All_Agents/*_requests.json
Shared_By_All_Agents/*_process.json
Shared_By_All_Agents/*_kill_list.json
Agents/*/Memory/
Documentation/Daily_Notes/
restart_requested.flag

# --- Databases ---
# The .bak-* forms matter: a hand-made backup like learning.db.bak-preD17 does
# NOT match *.db, and two of them sat uncovered in this public repo.
*.sqlite
*.sqlite3
*.db
*.db.bak*
*.db-*
*.sqlite.bak*

# --- Kage's project-managed Postgres cluster + gateway logs (T3/T4) ---
Start_Inky/pgdata/
Tools/*.log

# personal / local-only (never in public Kage repo)
personal-financial-blueprint.html
Gemini_Generated_Image_*.png
Screenshot *.png
Screens/Finance/finance-datamigration.md
current.png

# --- Claude Code session plans (ephemeral; outcomes land in PLAN.md) ---
.zcode/
.scratch/

# --- Private finance source docs + backfill data (NEVER commit) ---
*.pdf
LOAN ACCOUNT*.pdf
Screens/Finance/Backend/app/data/
qwen_agent_port/

# --- Anime screen: local-only, never on GitHub (restored from the old checkout) ---
Screens/Anime/

# --- Learning OS: owner's personal corpus (PII — resume, employers, schedule) ---
Screens/Learning/Context/

# --- AGENT DECK build scaffolding (Qwen turn scripts + apply state) ---
Stage_agents/

# --- LAN sync scripts (local tooling, phone address is personal) ---
Sync/

# --- Finance app build artifacts (mirrored by Screens/Finance/Backend/build.py) ---
Screens/Finance/Backend/app/static/
Screens/Finance/Backend/app/.serve.*

# --- Email card (D22): local mailbox cache + OAuth secrets ----------
Main_Menu/Backend/Email_Data/

# The Calendar card (D23) - Google Calendar tokens, the local store and
# the WakaTime key all live here. Same rule as Email_Data: nothing
# personal in git (CLAUDE.md Rule 7).
Main_Menu/Backend/Calendar_Data/

# Scratch downloads, logs and saved pages. Never committed - this is a
# public repo (Rule 7).
tmp/

# --- Personal resume files (PII, Rule 7) - never in the public repo ---
*.docx
Vivek_Kumar_Resume_*
