B-04 | code done, browser visual pass deferred (owner, <2 min) | Storage page: three trigger cards + `<dialog>` drawers (Code Structure / Agent Tree / Kage Data Schema) reading the B-03 endpoints (`STORAGE_TAB_SPEC.md §3–6`) | CHANGED `Screens/Storage/Page/page_for_storage.html` only (hand-rolled `cfg.PAGE`); no backend change | three `.card` buttons in one `.cards` grid row under a new STRUCTURE panel, each showing its spec summary (`11 screens · 169 modules · 0 violations` / `37 agents · 16 event types` / `1 database · 4 tables`) + `generated_at` age (`ageOf()`; amber `.stale` when >168h); one shared `<dialog class="drawer">` — `showModal()` gives Esc-close + one-at-a-time; `openDrawer(kind)` renders Modal A (screens tree + boundary map grouped by `from` with `http`/`import` via-badges + violations, red only when present) / Modal B (agent dots + orchestration + routing chains + edges + brokers, each empty section says so honestly) / Modal C (database accordions with column tables + FKs + views, 16-row spine-event table, markdown rules with `(absent)` marker, relationships); `state:never`/`error` → Regenerate button POSTing `/api/storage/structure/regenerate` then reloading; drawer slide animation is under `@media (prefers-reduced-motion: no-preference)` so reduced-motion gets none | VERIFIED headless: JS syntax clean (node vm), all 3 render fns run against the live 8009 endpoints without throwing (7.5k/4k/5.2k chars), card summaries + age/stale labels correct, regenerate returns `{state:ok, seconds:0.52}` | NOT VERIFIED: pixel pass in a real browser (Claude-in-Chrome extension not connected this session) — owner check at `127.0.0.1:8009`: 3 cards fit one row at 1440×900, a drawer opens <200 ms with no layout shift behind it, `prefers-reduced-motion` kills the slide, Esc closes, 390×844 the cards stack | regression `.venv\Scripts\python Start_Inky\run_checks.py` exit 0, all 10 checks PASS | commit: <pending>
