# What the Enhancement screen needs installed.
#
# Every screen keeps its own list, so one screen can be installed and run
# on its own without dragging in the others. Start_Everything.bat installs
# every one of these files it can find.
#
# Install just this one:
#   cd <repo root>
#   python -m pip install -r Screens\Enhancement\Setup\requirements_for_enhancement.txt

# The small web server behind this screen.
fastapi>=0.110
uvicorn[standard]>=0.27

# Nothing else. The board is plain Python and a JSON file - no database,
# no extra library.
